#!/usr/bin/env bash

[ $YADM_DIR ] || YADM_DIR="$XDG_CONFIG_HOME/yadm"
export GIT_DIR="$YADM_DIR/repo.git"

chosen=$(yadm ls-files $HOME | dmenu -i -p "Edit:") || exit

path=$(echo "$HOME/$chosen" | tr -d '[:space:]')

$TERMINAL -e $EDITOR $path
